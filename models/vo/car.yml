name: user
description: user view object
properties:
  - {
    name: name,
    type: string,
    from: entity.user.name
  }
  - {
    name: age,
    type: int,
    from: entity.user.name
  }
  - {
    name: car,
    type: vo.car,
    from:
  }
