name: user
description: user param
properties:
  - {
    name: name,
    type: string,
    require: true,
    search: entity.user.name,
  }
  - {
    name: age,
    type: int,
    require: false,
    search: entity.user.age,
  }
